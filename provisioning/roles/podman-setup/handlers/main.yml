---
# Podman Setup Role - Handlers

- name: Restart podman socket
  ansible.builtin.systemd:
    name: podman.socket
    state: restarted
    scope: user
  become: true
  become_user: "{{ project_user }}"
