; CallAnsible Asterisk Dialplan
; Generated by Ansible provisioning

[callansible]
; Main entry point - hand off to ARI
exten => 100,1,NoOp(Incoming call to CallAnsible)
 same => n,Answer()
 same => n,Stasis(voice-bridge)
 same => n,Hangup()

; Direct extension for testing
exten => 200,1,NoOp(Test extension)
 same => n,Answer()
 same => n,Playback(hello-world)
 same => n,Hangup()

; Echo test
exten => 300,1,NoOp(Echo test)
 same => n,Answer()
 same => n,Echo()
 same => n,Hangup()
